<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="amsi.dei.estg.ipleiria.pt.projeto">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:label="@string/app_name"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".e_main" />
        <activity android:name=".e_pedidos" />
        <activity android:name=".e_detalhesMensa" />
        <activity android:name=".e_notificacoes" />
        <activity android:name=".c_main" />
        <activity android:name=".c_itemListaementa" />
        <activity android:name=".c_ementa" />
        <activity android:name=".c_reservar_mesa" />
        <activity android:name=".c_contactos" />
        <activity android:name=".c_funcionalidades" />
        <activity android:name=".c_pedirbebida" />
        <activity android:name=".c_lista_pedidos"></activity>
    </application>

</manifest>